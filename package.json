{
  "name": "task-mgmt",
  "version": "1.0.0",
  "description": "Task Management Application with Playwright Tests",
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "dependencies": {
    "vite": "^5.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
} 